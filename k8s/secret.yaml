# TEMPLATE — do NOT commit with real values.
#
# Create the secret with kubectl:
#   kubectl create secret generic chat-app-secrets \
#     --namespace default \
#     --from-literal=ai-api-key=YOUR_KEY_HERE
#
# Or apply this file after replacing REPLACE_ME:
apiVersion: v1
kind: Secret
metadata:
  name: chat-app-secrets
  namespace: default
  labels:
    app: chat-app
type: Opaque
stringData:
  ai-api-key: "REPLACE_ME"
