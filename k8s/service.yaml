apiVersion: v1
kind: Service
metadata:
  name: chat-app
  namespace: default
  labels:
    app: chat-app
spec:
  type: ClusterIP
  selector:
    app: chat-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
